#!/bin/bash
# run rake with co-located jenkins-plugin.rb
# this makes the edit/run cycle faster by eliminating the need to rebuild jenkins-plugins gem
export RUBYLIB=../jenkins-plugins.rb/lib
exec rake "$@"

